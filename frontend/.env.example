# Скопируйте в .env для локальной разработки или задайте в Vercel Dashboard

# Production (Render) — можно с /api или без; суффикс /api добавится автоматически
VITE_API_URL=https://order-hcqr.onrender.com/api
VITE_API_BASE=https://order-hcqr.onrender.com/api

# Локальная разработка — оставьте пустым, используется Vite proxy /api → :8000
# VITE_API_URL=
# VITE_API_BASE=

VITE_TELEGRAM_BOT_TOKEN=
VITE_TELEGRAM_CHAT_ID=
